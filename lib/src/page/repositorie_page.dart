import 'package:flutter/material.dart';

class RepositoriePage extends StatelessWidget {
  const RepositoriePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
